{
  "name": "react-intro",
  "private": true,
  "scripts": {
    "dev": "lerna run dev",
    "test": "lerna run test",
    "build": "lerna run build",
    "start": "lerna run start"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^7.2.0"
  }
}
